interface:
  display_name: "Evidence Lane Exit Boot"
  short_description: "Close one governed session safely"
  default_prompt: "Use $evi-exit-boot to close this governed session and detach live runtime context safely."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
