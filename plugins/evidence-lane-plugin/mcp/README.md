# Evidence Lane MCP

This package binding creates the canonical package-local native MCP server. The `actions/` directory contains one hash-bound outer binding for every native action; the manifest binds configuration, implementation, SDK, counts, and schemas without duplicating handler logic.
