interface:
  display_name: "Link project evidence network"
  short_description: "Link hash-only summaries across projects"
  default_prompt: "Use $link-project-evidence-network to inspect or update the selected cross-project network."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
policy:
  allow_implicit_invocation: true
