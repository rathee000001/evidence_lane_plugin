interface:
  display_name: "Evidence Lane Boot"
  short_description: "Verify host, model, Flash, and resume"
  default_prompt: "Use $evi-boot to verify host/storage and model-tool compatibility, lock Flash, then boot or resume the exact governed Evidence Lane session."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
