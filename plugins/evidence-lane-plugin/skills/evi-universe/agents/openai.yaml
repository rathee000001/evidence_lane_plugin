interface:
  display_name: "Evidence Lane Universe"
  short_description: "Inspect Universe and connector-brain links"
  default_prompt: "Use $evi-universe to inspect bounded live Project Universe and connector-brain integrity evidence without merging authority roles."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
