interface:
  display_name: "EVI Brain Scaling"
  short_description: "Select bounded indexed brain slices"
  default_prompt: "Use $evi-brain-scaling to select a bounded indexed slice for this task."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
policy:
  allow_implicit_invocation: true
