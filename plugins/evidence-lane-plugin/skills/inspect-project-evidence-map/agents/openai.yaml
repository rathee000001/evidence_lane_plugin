interface:
  display_name: "Inspect project evidence map"
  short_description: "Inspect one project evidence and link map"
  default_prompt: "Use $inspect-project-evidence-map to inspect my selected project links and evidence map."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
