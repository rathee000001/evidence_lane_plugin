interface:
  display_name: "EVI Project Recipe"
  short_description: "Compile project-type execution recipes"
  default_prompt: "Use $evi-project-recipe to compile the exact project-type recipe for these sources."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
policy:
  allow_implicit_invocation: true
