interface:
  display_name: "Evidence Lane Build"
  short_description: "Bootstrap PV0 or build an unaccepted HIL proposal"
  default_prompt: "Use $evi-build for Source-Intake-backed PV0 or to construct and present an unaccepted Project plus Learning HIL proposal; use $evi-fuse for decisions."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
