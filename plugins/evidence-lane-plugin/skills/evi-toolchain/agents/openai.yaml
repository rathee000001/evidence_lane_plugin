interface:
  display_name: "EVI Full AI Toolchain"
  short_description: "Resolve conditional Codex toolchains"
  default_prompt: "Use $evi-toolchain to resolve the exact conditional toolchain for this lane."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
policy:
  allow_implicit_invocation: true
