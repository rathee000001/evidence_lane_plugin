interface:
  display_name: "Run project lifecycle"
  short_description: "Coordinate the complete project work lifecycle"
  brand_color: "#18A9C8"
  default_prompt: "Use $run-project-lifecycle to continue my project through its current workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
