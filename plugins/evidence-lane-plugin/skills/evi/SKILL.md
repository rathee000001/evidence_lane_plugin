---
name: evi
description: Evidence Lane 00 - open the complete persistent top-down /evi lifecycle from Boot through pointer-only Rollback. Use whenever the user starts, resumes, or asks to see the Evidence Lane flow.
---

# Evidence Lane ordered entry

Read and follow `../../commands/evi.md` completely. Preserve State Travel as
the conditional prepared-handoff preflight at the top of the static catalog.
Without a prepared handoff, execute atomic Boot/Flash as the first normal
action and show all seventeen returned source-intake commands. Preserve the
separate `/evi-mode` sidecar, automatic exit-Refresh transition,
exact-APPROVE Fuse gate, pointer-only Rollback law, and the separate explicit
`/evi-exit-boot` session deactivation.
