interface:
  display_name: "Evidence Lane"
  short_description: "Route governed Evidence Lane workflows"
  default_prompt: "Use $evi to route this request through the correct governed Evidence Lane workflow."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
