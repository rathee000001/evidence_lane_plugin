interface:
  display_name: "EVI Formula Engine"
  short_description: "Route bounded ENV/UOP formulas"
  default_prompt: "Use $evi-formula to compile and route this bounded ENV/UOP formula."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
policy:
  allow_implicit_invocation: true
