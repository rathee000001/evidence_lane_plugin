interface:
  display_name: "Revoke project connector"
  short_description: "Revoke project connector access with history"
  default_prompt: "Use $revoke-project-connector to revoke the selected connector grant."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
