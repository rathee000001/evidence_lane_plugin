interface:
  display_name: "Evidence Lane Memory"
  short_description: "Query and link the independent Project Memory DB"
  default_prompt: "Use $evi-memory to query bounded current Project Memory locators without merging other authorities."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
