interface:
  display_name: "Exchange task evidence"
  short_description: "Exchange attributed evidence between project tasks"
  default_prompt: "Use $exchange-task-evidence to inspect or send the selected task exchange."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
