interface:
  display_name: "Select project storage"
  short_description: "Inspect project roots and durable storage routes"
  default_prompt: "Use $select-project-storage to inspect or select my project storage root."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
