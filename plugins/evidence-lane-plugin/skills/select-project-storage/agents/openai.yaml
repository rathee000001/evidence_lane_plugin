interface:
  display_name: "Select project storage"
  short_description: "Inspect project roots and local persistence evidence"
  icon_small: "./assets/skill-cube.svg"
  icon_large: "./assets/skill-cube.svg"
  brand_color: "#18A9C8"
  default_prompt: "Use $select-project-storage to register or inspect my persistent local project root."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
