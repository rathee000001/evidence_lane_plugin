interface:
  display_name: "Configure project connector"
  short_description: "Configure a bounded project connector grant"
  default_prompt: "Use $configure-project-connector to configure the selected project connector."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
