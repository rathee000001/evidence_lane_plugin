interface:
  display_name: "Evidence Lane Rollback"
  short_description: "Move only the accepted PV pointer"
  default_prompt: "Use $evi-rollback to move only the accepted PV pointer to an immutable accepted version."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
