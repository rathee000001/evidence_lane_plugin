interface:
  display_name: "Close a project session"
  short_description: "Close a session and detach its capture binding"
  brand_color: "#18A9C8"
  default_prompt: "Use $close-project-session to close my current project session safely."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
