interface:
  display_name: "Evidence Lane Plan"
  short_description: "Bind a Codex plan to Plan Lane"
  default_prompt: "Use $evi-plan to bind the finished Codex plan to the canonical Evidence Lane Plan Lane and Goal."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
