interface:
  display_name: "EVI Bigger Universe"
  short_description: "Federate hash-only project mini-brains"
  default_prompt: "Use $evi-bigger-universe to register or explicitly link these project mini-brains."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
policy:
  allow_implicit_invocation: true
