interface:
  display_name: "Open project session"
  short_description: "Start or resume a verified project session"
  brand_color: "#18A9C8"
  default_prompt: "Use $open-project-session to start or resume my selected project session."
dependencies:
  tools:
    - type: "mcp"
      value: "evidence-lane"
      description: "Evidence Lane native MCP server"
