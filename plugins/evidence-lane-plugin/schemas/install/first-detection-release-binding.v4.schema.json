{
  "$defs": {
    "sha256": {
      "pattern": "^[0-9a-f]{64}$",
      "type": "string"
    }
  },
  "$id": "https://evidencelane.org/schemas/install/first-detection-release-binding.v4.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": true,
  "properties": {
    "assets": {
      "items": {
        "additionalProperties": false,
        "properties": {
          "asset_id": {
            "pattern": "^[a-z0-9-]+\\.part-[0-9]{3}-of-[0-9]{3}$",
            "type": "string"
          },
          "bytes": {
            "maximum": 2140000000,
            "minimum": 1,
            "type": "integer"
          },
          "component_id": {
            "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
            "type": "string"
          },
          "component_manifest_sha256": {
            "$ref": "#/$defs/sha256"
          },
          "condition": {
            "enum": [
              "always",
              "nvidia_cuda_compatible",
              "directml_compatible",
              "amd_rocm_compatible",
              "explicit_ghostscript_license"
            ]
          },
          "file_count": {
            "maximum": 100000,
            "minimum": 1,
            "type": "integer"
          },
          "filename": {
            "pattern": "^[A-Za-z0-9._-]+\\.zip$",
            "type": "string"
          },
          "part_count": {
            "maximum": 999,
            "minimum": 1,
            "type": "integer"
          },
          "part_index": {
            "maximum": 999,
            "minimum": 1,
            "type": "integer"
          },
          "payload_bytes": {
            "minimum": 1,
            "type": "integer"
          },
          "sha256": {
            "$ref": "#/$defs/sha256"
          },
          "url": {
            "pattern": "^https://github\\.com/rathee000001/evidence_lane_plugin/releases/download/",
            "type": "string"
          }
        },
        "required": [
          "component_id",
          "asset_id",
          "part_index",
          "part_count",
          "condition",
          "filename",
          "url",
          "bytes",
          "sha256",
          "component_manifest_sha256",
          "file_count",
          "payload_bytes"
        ],
        "type": "object"
      },
      "maxItems": 64,
      "type": "array"
    },
    "assets_sha256": {
      "$ref": "#/$defs/sha256"
    },
    "bundle_plan": {
      "const": "provisioning/full-bundle-plan.v4.json"
    },
    "bundle_plan_sha256": {
      "$ref": "#/$defs/sha256"
    },
    "installation_enabled": {
      "type": "boolean"
    },
    "plugin_id": {
      "const": "evidence-lane-plugin"
    },
    "plugin_version": {
      "const": "4.0.2"
    },
    "receipt_sha256": {
      "$ref": "#/$defs/sha256"
    },
    "release_ref": {
      "pattern": "^refs/tags/evidence-lane-v4\\.0\\.2-bundle-[0-9a-f]{16}$",
      "type": [
        "string",
        "null"
      ]
    },
    "repository": {
      "const": "https://github.com/rathee000001/evidence_lane_plugin"
    },
    "schema": {
      "const": "evidence-lane.first-detection-release-binding.v4"
    },
    "source_manifest": {
      "type": [
        "string",
        "null"
      ]
    },
    "source_manifest_sha256": {
      "anyOf": [
        {
          "$ref": "#/$defs/sha256"
        },
        {
          "type": "null"
        }
      ]
    },
    "sparse_root": {
      "const": "plugins/evidence-lane-plugin"
    },
    "status": {
      "enum": [
        "SOURCE_TEMPLATE_UNBOUND",
        "RELEASE_BOUND"
      ]
    }
  },
  "required": [
    "schema",
    "status",
    "installation_enabled",
    "plugin_id",
    "plugin_version",
    "repository",
    "sparse_root",
    "release_ref",
    "bundle_plan",
    "bundle_plan_sha256",
    "source_manifest",
    "source_manifest_sha256",
    "assets",
    "receipt_sha256"
  ],
  "title": "Evidence Lane first-detection release binding v4",
  "type": "object"
}
