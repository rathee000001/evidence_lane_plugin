export default function SupportPage() {
  return <main className="legal shell"><a href="/">← Evidence Lane</a><span className="kicker">Operational support</span><h1>Support and diagnostics</h1><p>Start with <a href="/healthz">/healthz</a>. A <code>BLOCKED</code> response is intentional when durable HTTPS origin, authentication, storage/queue, or exact release SHA is absent or mismatched.</p><h2>Connector endpoint</h2><p>The ChatGPT connector endpoint is <code>/mcp</code>. Opening it as a normal browser page is not a functional MCP test; use an MCP client or inspector with the required authentication.</p><h2>Release reports</h2><p>Every governed release should provide the Git SHA, deployment identity, package hashes, install evidence, test evidence, remaining blockers, and an unaccepted HIL candidate.</p></main>;
}
