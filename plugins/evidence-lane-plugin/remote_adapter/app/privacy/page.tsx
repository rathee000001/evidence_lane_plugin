export default function PrivacyPage() {
  return <main className="legal shell"><a href="/">← Evidence Lane</a><span className="kicker">Public policy</span><h1>Privacy</h1><p>The public landing page stores no Evidence Lane project data. The ChatGPT MCP edge forwards authenticated requests only to the configured durable origin and does not treat Vercel files or memory as state authority.</p><h2>Data handled</h2><p>When connected, the durable Evidence Lane service may process the source material and visible chat context a user intentionally supplies. Secrets are redacted where supported. Hidden chain-of-thought and private model reasoning are not Evidence Lane inputs.</p><h2>Retention and control</h2><p>Retention, deletion, access control, and audit receipts are governed by the configured durable service. If that service, authentication, or exact release identity cannot be verified, the edge blocks the request.</p></main>;
}
