---
description: Read the structured Docs source lane
argument-hint: <project_id> [query-or-path]
---

# /evi-09-docs

Resolve canonical lane `docs` through `lane_catalog`, call `lane_status`, then
use bounded `lane_search` or `lane_fetch`. Report document hierarchy, tables,
parser capability, exact source hash, and freshness.
