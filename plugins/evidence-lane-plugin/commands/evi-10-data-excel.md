---
description: Read the Data, Excel, and CSV source lane
argument-hint: <project_id> [query-or-path]
---

# /evi-10-data-excel

Resolve canonical lane `data_excel` through `lane_catalog`, call
`lane_status`, then use bounded `lane_search` or `lane_fetch`. Surface
workbook, sheet, formula, dependency, table, chart, CSV, parser, BM25, and
explicit TF-IDF evidence. Never imply unavailable optional tooling ran.
