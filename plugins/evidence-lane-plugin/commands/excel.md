---
description: Read the Excel, CSV, and structured-data lane
argument-hint: <project_id> [query-or-path]
---

# Evidence Lane Excel and data

Resolve `data_excel` through `lane_catalog`, call `lane_status`, then search or
fetch. Surface workbook/sheet structure, cells, formulas, dependency edges,
tables, charts, CSV shape, exact hashes, parser blockers, BM25, and explicit
TF-IDF. Never claim unavailable optional tooling ran.
