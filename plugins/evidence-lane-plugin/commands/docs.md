---
description: Read the structured documents lane
argument-hint: <project_id> [query-or-path]
---

# Evidence Lane documents

Resolve `docs` through `lane_catalog`, call `lane_status`, then use
`lane_search` or `lane_fetch`. Report document structure, headings, tables,
parser capability, exact bytes/hash, and freshness.
