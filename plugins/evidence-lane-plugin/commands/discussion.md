---
description: Read the discussion evidence lane
argument-hint: <project_id> [query-or-path]
---

# Evidence Lane discussion

Resolve `discussion` only through `lane_catalog`. Call `lane_status`, then
`lane_search` or `lane_fetch` for `$ARGUMENTS`. Report decisions, deltas, next
actions, exact source hashes, parser state, and freshness without mutation.
