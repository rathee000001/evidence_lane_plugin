---
description: Read the ordered Evidence Lane task backlog
argument-hint: <project_id>
---

# Evidence Lane task backlog

Call `pv_task_backlog` for `$ARGUMENTS`. Show ordered task IDs, exact contracts,
status counts, and the sole active task if one exists. Do not classify, reorder,
merge, or execute tasks.
