---
description: Add one of at most eight bounded persistent connector or AI-toolchain grants.
---

Load the `evi-additional-plugin` skill. This compatibility sidecar delegates to
the same governed connector brain as `/evi-plugin ADD:` and never changes Boot,
Exit Boot, HIL, Fuse, pointer, or State Travel state.
