---
description: Read the Project Engulf source lane
argument-hint: <project_id> [query-or-path]
---

# /evi-17-project-engulf

Resolve canonical lane `project_engulf` through `lane_catalog`, call
`lane_status`, then use bounded `lane_search` or `lane_fetch`. Read project
files, components, relationships, conflicts, and proposed sector targets.
Reading an engulf package never registers or imports it.
