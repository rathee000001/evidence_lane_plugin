---
description: Read the analysis evidence lane
argument-hint: <project_id> [query-or-path]
---

# Evidence Lane analysis

Resolve `analysis` through `lane_catalog`, call `lane_status`, then use
`lane_search` or `lane_fetch`. Keep claims, evidence, risks, alternatives, and
unknowns distinct. Do not turn interpretation into source fact.
