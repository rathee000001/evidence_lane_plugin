---
description: Read the custom-source lane
argument-hint: <project_id> [query-or-path]
---

# Evidence Lane custom sources

Resolve `custom` through `lane_catalog`, call `lane_status`, then search or
fetch. Show exact bytes/hash, parser choice, unsupported state, evidence,
decisions, and next actions. Do not silently reroute a source to another lane.
