---
description: Read the PPT and Presentation source lane
argument-hint: <project_id> [query-or-path]
---

# /evi-11-ppt

Resolve canonical lane `ppt` through `lane_catalog`, call `lane_status`, then
use bounded `lane_search` or `lane_fetch`. Read slides, notes, shapes, tables,
image references, hashes, and parser state without inferring unparsed content.
