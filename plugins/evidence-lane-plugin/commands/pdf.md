---
description: Read the PDF and OCR lane
argument-hint: <project_id> [query-or-path]
---

# Evidence Lane PDF and OCR

Resolve `pdf_ocr` through `lane_catalog`, call `lane_status`, then search or
fetch pages, native text blocks, images, OCR runs/blocks/lines, exact hashes,
and review regions. State whether OCR actually ran; missing OCR tools remain a
visible capability blocker, never an implied pass.
