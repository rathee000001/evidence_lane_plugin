---
description: Read the image and OCR lane
argument-hint: <project_id> [query-or-path]
---

# Evidence Lane images and OCR

Resolve `images_ocr` through `lane_catalog`, call `lane_status`, then search or
fetch image metadata and OCR evidence. Report the exact OCR engine/capability
state and source hash. Never describe metadata-only parsing as OCR success.
