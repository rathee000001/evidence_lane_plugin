---
description: Intake one exact local Git-backed source
argument-hint: <project_id> <absolute-local-git-path> <branch>
---

# /evi-03-local

Require successful `/evi-01-boot`, which atomically verifies Boot and locked
ENV/UOP Flash. Adopt one exact clean local Git repository through
`pv_enroll_project`, or read its registered `local_code` lane. Never crawl
neighboring paths, copy unrelated files, approve a candidate, or push.
