---
description: Read the project-engulf lane
argument-hint: <project_id> [query-or-path]
---

# Evidence Lane project engulf

Resolve `project_engulf` through `lane_catalog`, call `lane_status`, then search
or fetch project files, components, relationships, conflicts, and proposed
sector targets. Reading an engulf package never registers or imports it.
