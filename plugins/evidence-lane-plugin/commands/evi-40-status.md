---
description: Show the persistent Evidence Lane state envelope
argument-hint: <project_id>
---

# /evi-40-status

Call `pv_status`. Show source authority, accepted history, pointer generation,
entry PV, freshness, candidates, pending HIL, next ordinal, and backlog. Label
every candidate `UNACCEPTED_CANDIDATE`. Perform no mutation.
