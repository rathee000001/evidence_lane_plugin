---
description: Inspect or change the project-scoped primary storage connector with an exact receipt-backed selection.
---

Load the `evi-change-storage-connector` skill. This is the compatibility name for
the `/evi-storage` administrative sidecar and is not a seventh primary control.
