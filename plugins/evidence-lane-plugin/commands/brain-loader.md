---
description: Read the brain-loader package lane
argument-hint: <project_id> [query-or-path]
---

# Evidence Lane brain loader

Resolve `brain_loader` through `lane_catalog`, call `lane_status`, then search
or fetch package members, manifests, databases, schema objects, exact hashes,
and compatibility evidence. This command never activates or rewrites a brain.
