---
description: Read the operating-mode lane
argument-hint: <project_id> [query-or-path]
---

# Evidence Lane mode

Resolve `mode` through `lane_catalog`, call `lane_status`, then search or fetch
the exact operating rules, gates, allowed actions, and blocked actions. Do not
promote a mode document into ENV/UOP authority or mutate it.
