---
description: Read the Custom source lane
argument-hint: <project_id> [query-or-path]
---

# /evi-15-custom

Resolve canonical lane `custom` through `lane_catalog`, call `lane_status`,
then use bounded `lane_search` or `lane_fetch`. Show exact bytes, hashes,
parser choice, unsupported state, evidence, decisions, and next actions.
Never silently reroute the source.
