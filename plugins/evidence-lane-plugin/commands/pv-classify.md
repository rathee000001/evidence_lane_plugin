---
description: Create one bounded Evidence Lane task contract
argument-hint: <bounded task and exact scope>
---

# Evidence Lane task classification

Classify `$ARGUMENTS` once with `task_classify`.

Supply the exact outcome, task class, permitted paths/tools, executable
acceptance commands using the documented `cmd:` form, write boundary, and stop
condition. Report the returned contract. One backlog item becomes one Delta
and one HIL; never combine two tasks.
