# Evidence Lane tunnel

The executable tunnel host remains under `scripts/windows_tunnel/`. This surface hash-binds that host, its installer/manager, the required non-MCP runtime toolchain, hidden runtime root, project-id routing, and the temporarily retained maintainer helper.
