# project_universe

This is the installed schema/runtime projection of one non-sector authority. The SQLite file is an empty schema template built by the canonical implementation; MMD, DOT, tools, pointer contract, skills, commands, SDK/MCP actions, and source modules are hash-linked. Live project bytes remain under the user-selected Project/PV root.
