'use client';
import Link from'next/link';import{useState}from'react';import{EvidenceIcon}from'./evidence-brand';import{Orbit}from'./orbit';import{GlassOrb}from'./glass';import{ProjectDetail}from'./project-details';import{layers,sources,stages}from'../data/project-story';




export function HomeStory(){const[active,setActive]=useState(0);const[source,setSource]=useState(0);const[stage,setStage]=useState(0);const[continuity,setContinuity]=useState(0);const[popup,setPopup]=useState<{type:"layer"|"source"|"stage";index:number}|null>(null);return <main id="main" className="home-narrative">
<section className="cosmic-hero"><div className="hero-topline"><span>FOR WORK THAT OUTLIVES A CONVERSATION</span><span>EVIDENCE LANE / V4</span></div><div className="cosmic-copy"><p className="eyebrow">YOUR SOURCES. YOUR DIRECTION. YOUR PROJECT.</p><h1>Work moves on.<br/><em>The context<br/>comes with you.</em></h1><p>Evidence Lane connects your sources, Plan, tools and verified results in a persistent project for Codex. Pick up the next task with the evidence behind you—and a clear view of what comes next.</p><div className="actions"><a className="button primary" href="#project-system"><span className="glass-icon-orb"><img src="/assets/tool-icons/node.svg" width="20" height="20" alt=""/></span>Explore your project ↓</a><a className="text-link" href="#why">Why it exists ↘</a></div></div><div className="cosmic-system"><Orbit sceneName="evidence-orbit" variant={active*.06}/><div className="orbital-labels" aria-label="Explore an evidence fragment">{layers.map(([name,,,color],i)=><button key={name} className={`fragment-label fragment-${i}`} onClick={()=>{setActive(i);setPopup({type:"layer",index:i})}} aria-haspopup="dialog"><GlassOrb color={color} icon={["database","package","terminal","git","node","python"][i]} size={44}/><span>{name}</span></button>)}</div><div className="core-label"><span>YOUR PROJECT</span><strong>{layers[active][0]}</strong><small>CONNECTED CONTEXT · SEPARATE RESPONSIBILITIES</small></div></div><div className="hero-layer-picker" aria-label="Select a project layer">{layers.map(([name,,,color],i)=><button key={name} onClick={()=>setActive(i)} aria-pressed={i===active}><i style={{background:color}}/>{name}</button>)}</div><div className="hero-bottom"><span>ILLUSTRATIVE PROJECT · SELECT A FRAGMENT TO EXPLORE</span><a href="#project-system">SCROLL TO EXPLORE ↓</a></div></section>
<section className="project-system wide-shell" id="project-system"><div><p className="eyebrow">01 / A PROJECT WITH ITS CONTEXT INTACT</p><h2>Everything connected.<br/><em>Every part accountable.</em></h2><p className="body-large">Explore the parts of your project. Each carries useful context forward without taking over another part’s responsibility.</p></div><div className="system-explorer"><div className="system-buttons">{layers.map(([name,,,color],i)=><button key={name} onClick={()=>{setActive(i);setPopup({type:"layer",index:i})}} aria-haspopup="dialog" aria-pressed={active===i}><GlassOrb color={color} icon={["database","package","terminal","git","node","python"][i]} size={36}/>{name}<small>0{i+1}</small></button>)}</div><article aria-live="polite"><span className="eyebrow">{layers[active][0]}</span><h3>{layers[active][1]}</h3><p>{layers[active][2]}</p><Link href="/product">Explore the project atlas ↗</Link></article></div></section>
<section className="problem-story wide-shell" id="why"><div><p className="eyebrow">02 / THE PROBLEM</p><h2>The work is still there.<br/><em>The context often isn’t.</em></h2><p className="body-large">The next conversation starts with “let me explain.” Files have changed. Decisions are scattered. A confident answer leaves you asking what it actually read.</p><p className="body-large">Evidence Lane gives the project a durable place to keep its sources, current work and recorded results—so the next step can begin from evidence.</p></div><div className="problem-comparison">{[['You re-explain the project','Resume with selected project context.'],['The same files get read again','Query prepared evidence; refresh the selected changes.'],['A result arrives without its trail','Inspect inputs, output and recorded checks together.'],['The next action loses its boundaries','Continue from the current Plan, scope and permissions.']].map(([before,after],i)=><article key={before}><span>0{i+1}</span><div><p>{before}</p><h3>{after}</h3></div></article>)}</div></section>
<section className="source-story" id="source-story"><div className="wide-shell"><p className="eyebrow">03 / BRING THE WORK YOU ALREADY HAVE</p><h2>Different sources.<br/><em>A shared project story.</em></h2><div className="source-tabs" aria-label="Explore source families">{sources.map(([name,,,,icon],i)=><button key={name} onClick={()=>{setSource(i);setPopup({type:"source",index:i})}} aria-haspopup="dialog" aria-pressed={source===i}><span className="glass-icon-orb"><img src={`/assets/tool-icons/${icon}.svg`} width="22" height="22" alt=""/></span>{name}</button>)}</div><div className="source-story-body"><div className="source-sculpture"><Orbit compact sceneName="atlas" variant={source*.2}/><span>SELECTED SOURCES → ONE PROJECT PATH</span></div><article aria-live="polite"><p className="eyebrow">{sources[source][1]}</p><h3>{sources[source][0]} that stays connected to its origin.</h3><p>{sources[source][2]}</p><div className="output-note"><span>WHAT YOU GET</span><p>{sources[source][3]}</p></div><Link href="/integrations">Explore supported capabilities ↗</Link></article></div></div></section>
<section className="workflow-story wide-shell" id="workflow-story"><p className="eyebrow">04 / THE END-TO-END WORKFLOW</p><h2>From a clear intention<br/><em>to a traceable result.</em></h2><div className="story-stepper" aria-label="Explore the project workflow">{stages.map(([title],i)=><button key={title} aria-pressed={stage===i} aria-haspopup="dialog" onClick={()=>{setStage(i);setPopup({type:"stage",index:i})}}><span>0{i+1}</span>{title}</button>)}</div><div className="story-stage" aria-live="polite"><article><p className="eyebrow">{stages[stage][0]}</p><h3>{stages[stage][1]}</h3><p>{stages[stage][2]}</p><Link href="/how-it-works">Follow the complete journey →</Link></article><div className="stage-flow" key={stage}><div>{stages[stage][3]}</div><span className="energy-line"/><EvidenceIcon large/><span className="energy-line"/><div>{stages[stage][4]}</div></div></div><p className="quiet">Choose from 24 public workflows for source intake, planning, work, recall, connections, recovery and continuity.</p><Link className="button" href="/workflows">Find the workflow for your next step ↗</Link></section>
<section className="continuity-story" id="continuity"><div className="wide-shell"><div><p className="eyebrow">05 / KEEP GOING WITHOUT LOSING THE THREAD</p><h2>A new conversation.<br/><em>The same project.</em></h2><p className="body-large">Your session can change without treating the project as a blank page. Read its saved context, inspect what changed and continue only when the current boundaries agree.</p></div><div className="continuity-console"><div className="console-heading"><EvidenceIcon/><span>PROJECT CONTINUITY</span><small>Illustrative example</small></div><div className="continuity-controls">{['Work recorded','Session resumed','Next step ready'].map((s,i)=><button key={s} onClick={()=>setContinuity(i)} aria-pressed={continuity===i}>{s}</button>)}</div><div className="continuity-readout" aria-live="polite"><span>PROJECT / QUARTERLY REPORT</span><h3>{['The report has a recorded result.','The next session checks the saved context.','Continue from the current Plan.'][continuity]}</h3><p>{['Sources, task scope, output references and checks remain together.','Project identity, session references and changed evidence are checked before resuming.','If the sources changed, refresh them. If the outcome changed, steer the Plan at a safe boundary.'][continuity]}</p><dl><div><dt>Source evidence</dt><dd>Attributed references</dd></div><div><dt>Completed work</dt><dd>History preserved</dd></div><div><dt>Next action</dt><dd>{['Read current state','Verify context','Follow eligible task'][continuity]}</dd></div></dl></div></div></div></section>
<section className="studio-story wide-shell"><div className="studio-story-art"><Orbit compact sceneName="studio"/></div><div><p className="eyebrow">06 / A VISIBLE WINDOWS STUDIO</p><h2>See the work.<br/><em>Keep your bearings.</em></h2><p className="body-large">Follow the Plan, jobs, workers, evidence, toolchains and connections. Studio is a read-only observatory; you direct project changes through Codex.</p><Link className="button primary" href="/studio">Look inside Studio ↗</Link></div></section>
<section className="usecases wide-shell"><p className="eyebrow">07 / WHERE THE PIECES COME TOGETHER</p><h2>Real projects rarely<br/><em>fit in one file.</em></h2><div>{[['Develop a feature','Connect the repository, requirements and current Plan. Inspect the selected changes and the checks that ran.','Code + documents + project checks'],['Build a report','Bring together selected spreadsheets, research and presentation material. Keep source references with the final artifact.','Excel + research + Word / PowerPoint'],['Investigate a question','Read selected PDFs, web sources and datasets with attributed extraction. Keep uncertainty and coverage visible.','PDF + web + structured data']].map(([title,text,tags])=><article key={title}><span className="eyebrow">{tags}</span><h3>{title}</h3><p>{text}</p></article>)}</div></section>
<section className="faq-story wide-shell"><div><p className="eyebrow">BEFORE YOU BEGIN</p><h2>A few useful<br/><em>distinctions.</em></h2></div><div>{[['Is this another chat app?','Evidence Lane is a project workflow plugin for Codex with a persistent engine. Windows Studio makes project observations visible. You continue to direct work through Codex.'],['Where does my project data live?','Source files stay in the selected source root. Project state lives in a separate selected durable root, with distinct databases and files for each owning part. Configured remote engines and external services have separate access boundaries.'],['Can Studio change my project?','No. Humans can view, navigate, open and minimize Studio. Project changes, jobs and connection configuration go through the authorized Codex workflows.'],['Does it use every listed tool?','No. The selected operation determines the eligible primary route and declared alternatives. Measured readiness, permissions, compatibility and budgets are checked before invocation.'],['Can I install this version now?','This website describes the 4.0.9 experimental snapshot. Public release integration and installed-runtime qualification remain pending in the inspected source contracts. Use the release guide to distinguish a source tag from a qualified install.']].map(([q,a])=><details key={q}><summary>{q}</summary><p>{a}</p></details>)}</div></section>
<section className="final-invitation wide-shell"><EvidenceIcon large/><p className="eyebrow">KEEP THE CONTEXT. MOVE THE PROJECT FORWARD.</p><h2>Your next step<br/><em>starts with the right evidence.</em></h2><div className="actions"><Link className="button primary" href="/docs/quickstart">Read the Quickstart ↗</Link><Link className="button" href="/download">Check release availability →</Link></div></section><ProjectDetail selection={popup} onClose={()=>setPopup(null)}/></main>}
