'use client';import{useState}from'react';import Link from'next/link';import integrations from'../data/integrations.json';
export function Harbor(){const[category,setCategory]=useState('All');const[query,setQuery]=useState('');const items=integrations.filter(i=>(category==='All'||category===i.category)&&`${i.name} ${i.operations}`.toLowerCase().includes(query.toLowerCase()));return <section className="shell"><div className="explorer-toolbar"><label>Find a capability<input type="search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search documents, code, OCR…"/></label><label>Source family<select value={category} onChange={e=>setCategory(e.target.value)}>{['All','Code','Office','Data & BI','Research & media','Sources'].map(c=><option key={c}>{c}</option>)}</select></label><span role="status">{items.length} capabilities</span></div><div className="capability-grid">{items.map(i=><article key={i.name}><div className="capability-title"><span className="capability-icon" aria-hidden="true">{i.glyph}</span><div><small>{i.category}</small><h2>{i.name}</h2></div></div><p>{i.operations}</p><dl><dt>Source support</dt><dd>Registered operations</dd><dt>Configuration</dt><dd>{i.configuration}</dd><dt>Measured readiness</dt><dd>Not measured on this website</dd></dl><details><summary>Limits & supported operations</summary><p>{i.limitation}</p><p className="technical">{i.actions.join(' · ')}</p></details></article>)}</div>{!items.length&&<p className="boundary" role="status">No matching capability. Try a broader search or select All.</p>}<div className="section"><h2>Connections have a purpose.<br/><em>Access has a scope.</em></h2><p className="body-large">External connections require an existing registered adapter, explicit resource scope, configuration and current grants. Local tools, connected services and compute providers have separate readiness checks.</p><div className="actions"><Link className="button" href="/docs/integrations">Read the integration guide →</Link><Link href="/docs/workflows/configure-project-connector">Configure a connection →</Link></div></div></section>}
