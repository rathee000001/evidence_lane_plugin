import type {Metadata} from 'next';import type {ReactNode} from 'react';import './globals.css';import './components/neon-glass.css';import {Header,Footer} from './components/shell';import {Experience} from './components/experience';
export const metadata:Metadata={title:{default:'Evidence Lane — Keep the context. Move the project forward.',template:'%s | Evidence Lane'},description:'Connect selected sources, a current Plan and verified results in a persistent project for Codex, with a visible read-only Windows Studio.',robots:{index:true,follow:true},icons:{icon:'/evidence-lane-icon.png'}};
export default function RootLayout({children}:{children:ReactNode}){return <html lang="en" data-scroll-behavior="smooth"><body suppressHydrationWarning><Experience><Header/>{children}<Footer/></Experience></body></html>}
